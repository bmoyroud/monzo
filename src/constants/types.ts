export const accounts = ["uk_prepaid", "uk_retail", "uk_retail_joint"] as const;

export const feedItems = ["basic"] as const;

// TODO: add all currencies?
export const currencies = ["GBP", "USD"] as const;
